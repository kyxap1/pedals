# Copy changes — Old Blood Noise Endeavors MTET

Source: `00794+-+001OBNE+-+Midi+to+Expression+Translator+-+Instruction+Manual+01.17.1.pdf`,
kept in this directory. The PDF's text is converted to outlines, so every
passage was read off the page renders by eye; `pdftotext` returns nothing.

The page was built in an earlier session that left no log. This file records
only the departures introduced by the figure-repair pass; the body copy was
verified word for word against the renders and matches the print.

## Wording

- **MIDI Command Table footer** — printed `oldbloodnoise.com`, page links it to
  `https://oldbloodnoise.com`. Printed text kept.

## Section headings

The manual prints no headings for its first three sections (the panels run as
continuous prose). `Introduction`, `Message System` and `Expression Setup` are
editorial headings cut at the document's own seams, so the page has an outline
and a TOC. `FAQ and Troubleshooting`, `MIDI COMMAND TABLE` and
`MTET USER NOTES` are the manual's own wording.

## Figures

- The masthead flourishes come from p1: the four-dot row above the wordmark,
  the three-dot row below it. An earlier crop used p6's two-row flourish in
  both slots.
- The glyph between `oldbloodnoise.com` and `2023` is the drop printed on p9,
  not the OB/NE wordmark.
- `THRU` above the MIDI THRU jack, `There is one special CC message: CC 100.`
  and the FAQ questions are live text; only the drawings are images.
- The `9v 50mA` label stays inside `power-icon.png`: it is a legend integral to
  the connector pictogram, not prose.
